# CLAUDE.md — maestro 저장소 절대규칙 (자동 적용)

이 파일은 Claude Code 세션이 이 저장소에서 열릴 때 **자동으로 로드**된다.
아래 규칙은 **별도 지시가 없어도 항상 적용되는 절대규칙**이다. 사용자가 명시적으로
바꾸라고 하기 전까지 어기지 말 것. (프로젝트: LOSTBALL 로스트볼 선별설비 — Three.js
시뮬 `lostball_simulation.html` 기반 부품 제작·발주.)

---

## 0. 대화·문서 형식
- **채팅(대화) 설명**에서 전문용어는 `원어(한국어:설명)` 형식으로 표기한다. 예: `fillet(필렛:모서리 둥글림)`.
- **산출 문서(.docx/.xlsx) 안에는** 이 표기·용어집 섹션을 **넣지 않는다**(문서는 실무용 본문만).
- 산출물은 **문서 파일(.docx/.xlsx/STL)**로 만든다. HTML 아티팩트로 대체하지 않는다.
- 모든 발주 문서는 **한국어(KR)·중국어(CN) 2판**으로 생성한다.
- 세밀도는 **"질문이 필요 없는 수준"**. BOM 등은 부품번호 규칙으로 기본문서화한다.
  - 부품번호: `LBH-[P철판/F각파이프/C캐스터/R보강/B체결/E부속]-NN`.

## 1. 분류·발주 정책
- 전 부품을 **3D프린트 / 외주가공**으로 분류한다.
- **호퍼는 엘레베이터(핵심 침수버킷 승강기구)와 분리**하여 중국 제작업체에 발주한다(보안:
  단일 업체가 핵심 기구 전체를 못 보게).
- 호퍼 발주 범위: **인버터(inverter)만 제외**하고, 호퍼에 볼팅된 철판·보강판·체결판·
  전장함(電裝函:컨트롤박스) 등 **연결된 금속은 전부 포함**. 엘레베이터·작업대와 연결돼
  있어도 **호퍼와 연결됐으면 호퍼부품으로 인정**한다.
- 각파이프는 시스템 전체를 **통합**하여 호퍼 제작업체에 **턴키** 발주한다.
- 재질: 호퍼 철판 **SUS201**(가성비 — SUS304는 산업용에 오버스펙, 쓰지 말 것),
  프레임 각파이프 **STK400**. 밀도 7800 kg/m³로 중량 산출.

## 2. 3D프린트 STL 절대규칙
1. **개별 분리**: 조립되어 합쳐진 덩어리는 부품 단위로 **전부 개별 STL**로 분리한다.
   (프린트 시 합쳐 배치하는 것은 별개 문제. 조립되는 부품은 모두 별개다.)
2. **중복 통합**: 회전·미러·이동 불변으로 동일한 모델은 **1개만** 만들고 **필요수량 표기**.
3. **섬스크류(thumbscrew:손나사) 1종 통일**: 헤드(Ø12×6 널링 18각)+**나사부 전체** 구현.
   프린트용 굵은 피치 **M5×P2.0**(금속 0.8의 약 2.5배). 소켓/너트(암나사)도 **동일 P2.0**,
   체결여유 +0.4. 시뮬에 여러 종이 있으면 **1종으로 통일**해서 구현한다.
4. **홀은 실측 위치·기능별**로 절삭한다:
   - 관통(THROUGH+NUT) = **clearHole**로 관통.
   - 탭(TAP) = **tapDrill**로 **블라인드**(막힌 구멍).
5. **파라메트릭 재구성**: 솔리드화 불가한 부품(얇은 셸 등)은 코드 실측치수로
   **솔리드+홀** 재구성한다.
6. **재질 분류는 발주측이 한다** → 재질별 폴더로 나누지 말고 **일괄 생성**(재질은 hint만 표기).
7. **전량 수밀(watertight:닫힌 솔리드)·1바디**여야 한다. 아니면 납품 금지.

## 3. 살덧붙임(Wall Reinforcement) — 절대규칙 ★
기준 품질: `fabrication/RULE_3DP_WALL_REINFORCE.md`, 참조 STL `cold_air_duct_fused`
(수밀 1바디·균일벽·엣지 필렛). **STL 세트를 생성/수정할 때마다 마지막에 반드시 실행**한다.

절차(구현: `fabrication/scripts/reinforce2.py`, 강제 실행: `fabrication/scripts/enforce_rules.py`):
1. 각 부품 표면을 ray 벽두께로 스캔(`trimesh.proximity.thickness(..., exterior=False, method='ray')`).
2. 지표 `p1`(1퍼센타일)·`<1.5mm 비율`로 얇은 부품 판정(합격선 대략 f15 ≤ 1.5%, p1 ≥ 1.2mm).
3. **오검 제외**: 나사산 크레스트(원래 얇음, 소켓/섬스크류 축 반경 내)·기능성 챔퍼(p1≥1.5mm)는 보강 금지.
4. 실제 얇은 부품만 **복셀 팽창(Minkowski 외측 오프셋 = 살 추가 + 샤프엣지 필렛)** 후
   **중심 정렬로 원위치 보존** → **기능 홀 재절삭**(clearHole/tapDrill, 섬스크류 소켓 P2.0).
5. 채택 조건: `수밀 && 1바디 && p1 개선 && 홀수 일치`. 미달이면 **원본 유지**(파괴적 변경 금지).
6. 복셀 재메시로 면수가 폭증하면 `fast_simplification`으로 데시메이션(수밀 유지).

## 4. 파이프라인 & 재현
`fabrication/scripts/`: 추출(Playwright) → 중복통합 → 체결/섬스크류 실측 → 나사엔진(threadlib)
→ 홀 절삭 → 파라메트릭 재구성 → **reinforce2(살덧붙임)** → docx 명세.
필요 패키지: `trimesh manifold3d rtree scipy scikit-image fast-simplification python-docx openpyxl`.
스크립트의 `SP=` 절대경로는 로컬 작업경로로 수정해서 재실행한다.

## 5. Git
- 지정 브랜치에서 작업·커밋·푸시하고 드래프트 PR을 만든다.
- 커밋/PR 본문은 **한국어**로 쓴다(요청에 따름). 모델 식별자·비밀정보는 저장소에 남기지 않는다.
